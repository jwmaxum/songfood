# [Phase 7] 시스템 통합 테스트, 보안 검증 및 운영 모니터링 고도화 (improve7.md)

## 1. 개요 및 목적
구축된 식품 마스터 라벨링 시스템의 품질, 규제 안전성, 보안 및 대량 데이터 처리 성능을 보장하기 위해 **자동화 단위/통합 테스트(Jest)**, **관리자 역할 기반 접근 제어(RBAC & RLS)**, **라벨 수정 감사 로그(Audit Log)** 및 **대량 300품목 일괄 검증 파이프라인**을 완성하고 운영 체계를 수립합니다.

---

## 2. 자동화 테스트 스위트 설계 (`src/__tests__/`)

### 2.1. 단위 테스트 (Unit Tests)
1. **`compliance-us.test.ts` (미국 규제 테스트)**:
   - 육류(소고기/돼지고기) 2% 초과 시 USDA 관할 경고 발생 여부 테스트.
   - 참깨(Sesame) 포함 시 9대 알레르겐 'CONTAINS' 표기 필수 검증.
   - 첨가당(Added Sugars) 누락 시 검증 실패 확인.
2. **`compliance-cn.test.ts` (중국 규제 테스트)**:
   - GACC 18자리 코드 유효성 정규식 검사.
   - 5대 영양성분 및 NRV% 산출 정확도 검증.
3. **`compliance-jp.test.ts` (일본 규제 테스트)**:
   - 8대 특정원재료(호두 포함) 검출 및 식염상당량($\text{Na} \times 2.54 / 1000$) 계산 정밀도 검증.
4. **`compliance-eu.test.ts` (EU 규제 테스트)**:
   - 14대 알레르겐 시각적 강조 태그 검증 및 kJ/kcal 환산 공식 검증.
5. **`compliance-uae.test.ts` (UAE 규제 테스트)**:
   - 잔류 알코올 0.05% 초과 감지 및 할랄 인증 여부 검증.
6. **`nutrition-calculator.test.ts` (영양성분 환산 테스트)**:
   - 100g ➔ 1회 제공량 변환 시 부동소수점 오차 방지 및 반올림 규정 준수 검증.

### 2.2. E2E 워크플로우 검증 시나리오
1. 신규 제품 등록 ➔ 5개국 라벨 자동 생성 ➔ Red-Flag 검출 ➔ 스마트 번역 및 자동 수정(Auto-Fix) 적용 ➔ 100점 컴플라이언스 통과 ➔ 바이어 PDF 스펙시트 다운로드 완료까지의 무결점 통과 검증.

---

## 3. 보안 및 권한 제어 (Security & RBAC)

1. **라벨 버전 관리 (Versioning)**:
   - 라벨 내용 수정 시 이전 버전(`version = 1`)을 보존하고 신규 버전(`version = 2`)을 생성하여 규제 이력 추적 가능.
2. **감사 로그(Audit Trail)**:
   - `label_audit_logs` 테이블을 통해 수정자, 변경 시각, 수정 전/후 Diff 데이터 영구 기록.
3. **Row Level Security (RLS)**:
   - 비인증 사용자(바이어): `is_compliant = true` 상태인 공개 스펙시트만 읽기 전용 접근.
   - 인증된 관리자: 모든 상태의 라벨 데이터 CRUD 가능.

---

## 4. 300여 개 품목 대량 일괄 처리 (Batch Processing)
- **일괄 규제 재검증(Bulk Re-validation)**:
  - 수입국 규제 법안이 개정될 경우(예: 알레르겐 목록 추가), 300개 전체 품목에 대해 10초 이내에 일괄 재검증을 수행하고 영향받는 품목 리포트 자동 생성.
- **백그라운드 비동기 처리**: 대용량 엑셀 업로드 및 PDF 배치 생성 시 브라우저 멈춤 없는 비동기 웹 워커 또는 API 스트리밍 처리.

---

## 5. 외부 규제 DB (aT KATI 등) 연동 인터페이스 가이드
- aT KATI 농식품수출정보 API 및 글로벌 법규 업데이트 피드를 수신할 수 있는 웹훅(Webhook) 엔드포인트 `/api/labels/regulatory-feed` 규격 정의.

---

## 6. 실행 및 검증 기준 (Done Criteria)
1. `npm run test` 실행 시 라벨링 관련 모든 테스트 케이스(15개 이상)가 100% 통과(PASS)할 것.
2. `npm run build` 실행 시 Turbopack/Next.js 빌드 에러 0건 유지.
3. 실 운영 배포 체크리스트 및 운영자용 퀵 매뉴얼(`LABEL_OPERATIONS_MANUAL.md`) 작성 완료.
